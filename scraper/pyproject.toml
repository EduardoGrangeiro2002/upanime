[project]
name = "upanime-scraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.40",
    "playwright-stealth>=1.0",
    "yt-dlp>=2024.0",
    "httpx>=0.27",
    "certifi>=2026.2.25",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
